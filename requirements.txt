
# MkDocs material theme
mkdocs-material

# awesome-pages plugin from PyPI
mkdocs-awesome-pages-plugin

# NOTE: mkdocs-prev-next-plugin was removed from requirements because the
# upstream source could not be reliably fetched in CI. If you want prev/next
# links, either add a compatible plugin here (pin to a released wheel or a
# GitHub release zip), or vendor the plugin code in the repo.
