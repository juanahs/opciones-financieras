# Pin MkDocs to 1.x to avoid MkDocs 2.0 breaking changes (Material + plugins require MkDocs 1.x)
mkdocs>=1.4,<2

# MkDocs material theme
mkdocs-material

# awesome-pages plugin from PyPI
mkdocs-awesome-pages-plugin

# Previous/next links are provided by Material's navigation.footer feature;
# no additional plugin is required.
